# CHICKEN-RUN

## Dependencies

I used a `PostgreSQL` database.

I used these libraries:
  - express
  - cors
  - pg
  - pg-prepared

## Usage

Run: `node chicken.js`

The server is now launched on `http://localhost:8000/`
